# Dependencia de sistema (no instalable con pip): python3-tk
# Necesaria para que matplotlib pueda mostrar ventanas (plt.show()) en vez de caer al backend
# no interactivo Agg. Instalar con: sudo apt install python3-tk
tensorflow>=2.13.0
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.3.0
scipy>=1.10.0
Pillow>=10.0.0
